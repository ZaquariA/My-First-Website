
/*This is where row 2 starts!!!*/
document.addEventListener("keydown", function(highlight) {
    if(highlight.code === "Backquote") {
    const Key = document.querySelector("#bckquoteKey");
    Key.style.background = "lightblue";
    }
});
document.addEventListener("keyup", function(highlight) {
    if(highlight.code === "Backquote") {
    const Key = document.querySelector("#bckquoteKey");
    Key.style.background = "lightgrey";
    }
});
document.addEventListener("keydown", function(highlight) {
    if(highlight.code === "Digit1") {
    const Key = document.querySelector("#oneKey");
    Key.style.background = "lightblue";
    }
});
document.addEventListener("keyup", function(highlight) {
    if(highlight.code === "Digit1") {
    const Key = document.querySelector("#oneKey");
    Key.style.background = "lightgrey";
    }
});
document.addEventListener("keydown", function(highlight) {
    if(highlight.code === "Digit2") {
    const Key = document.querySelector("#twoKey");
    Key.style.background = "lightblue";
    }
});
document.addEventListener("keyup", function(highlight) {
    if(highlight.code === "Digit2") {
    const Key = document.querySelector("#twoKey");
    Key.style.background = "lightgrey";
    }
});
document.addEventListener("keydown", function(highlight) {
    if(highlight.code === "Digit3") {
    const Key = document.querySelector("#threeKey");
    Key.style.background = "lightblue";
    }
});
document.addEventListener("keyup", function(highlight) {
    if(highlight.code === "Digit3") {
    const Key = document.querySelector("#threeKey");
    Key.style.background = "lightgrey";
    }
});
document.addEventListener("keydown", function(highlight) {
    if(highlight.code === "Digit4") {
    const Key = document.querySelector("#fourKey");
    Key.style.background = "lightblue";
    }
});
document.addEventListener("keyup", function(highlight) {
    if(highlight.code === "Digit4") {
    const Key = document.querySelector("#fourKey");
    Key.style.background = "lightgrey";
    }
});
document.addEventListener("keydown", function(highlight) {
    if(highlight.code === "Digit5") {
    const Key = document.querySelector("#fiveKey");
    Key.style.background = "lightblue";
    }
});
document.addEventListener("keyup", function(highlight) {
    if(highlight.code === "Digit5") {
    const Key = document.querySelector("#fiveKey");
    Key.style.background = "lightgrey";
    }
});
document.addEventListener("keydown", function(highlight) {
    if(highlight.code === "Digit6") {
    const Key = document.querySelector("#sixKey");
    Key.style.background = "lightblue";
    }
});
document.addEventListener("keyup", function(highlight) {
    if(highlight.code === "Digit6") {
    const Key = document.querySelector("#sixKey");
    Key.style.background = "lightgrey";
    }
});
document.addEventListener("keydown", function(highlight) {
    if(highlight.code === "Digit7") {
    const Key = document.querySelector("#sevenKey");
    Key.style.background = "lightblue";
    }
});
document.addEventListener("keyup", function(highlight) {
    if(highlight.code === "Digit7") {
    const Key = document.querySelector("#sevenKey");
    Key.style.background = "lightgrey";
    }
});
document.addEventListener("keydown", function(highlight) {
    if(highlight.code === "Digit8") {
    const Key = document.querySelector("#eightKey");
    Key.style.background = "lightblue";
    }
});
document.addEventListener("keyup", function(highlight) {
    if(highlight.code === "Digit8") {
    const Key = document.querySelector("#eightKey");
    Key.style.background = "lightgrey";
    }
});
document.addEventListener("keydown", function(highlight) {
    if(highlight.code === "Digit9") {
    const Key = document.querySelector("#nineKey");
    Key.style.background = "lightblue";
    }
});
document.addEventListener("keyup", function(highlight) {
    if(highlight.code === "Digit9") {
    const Key = document.querySelector("#nineKey");
    Key.style.background = "lightgrey";
    }
});
document.addEventListener("keydown", function(highlight) {
    if(highlight.code === "Digit0") {
    const Key = document.querySelector("#zeroKey");
    Key.style.background = "lightblue";
    }
});
document.addEventListener("keyup", function(highlight) {
    if(highlight.code === "Digit0") {
    const Key = document.querySelector("#zeroKey");
    Key.style.background = "lightgrey";
    }
});
document.addEventListener("keydown", function(highlight) {
    if(highlight.code === "Minus") {
    const Key = document.querySelector("#minusKey");
    Key.style.background = "lightblue";
    }
});
document.addEventListener("keyup", function(highlight) {
    if(highlight.code === "Minus") {
    const Key = document.querySelector("#minusKey");
    Key.style.background = "lightgrey";
    }
});
document.addEventListener("keydown", function(highlight) {
    if(highlight.code === "Equal") {
    const Key = document.querySelector("#equalKey");
    Key.style.background = "lightblue";
    }
});
document.addEventListener("keyup", function(highlight) {
    if(highlight.code === "Equal") {
    const Key = document.querySelector("#equalKey");
    Key.style.background = "lightgrey";
    }
});
document.addEventListener("keydown", function(highlight) {
    if(highlight.code === "Backspace") {
    const Key = document.querySelector("#bckspcKey");
    Key.style.background = "lightblue";
    }
});
document.addEventListener("keyup", function(highlight) {
    if(highlight.code === "Backspace") {
    const Key = document.querySelector("#bckspcKey");
    Key.style.background = "lightgrey";
    }
});
/* This is where row 3 starts!!!*/
document.addEventListener("keydown", function(hightlight) {
    if(hightlight.key === "Tab") {
    const TabKey = document.querySelector("#tabKey");
    TabKey.style.background = "lightblue";
    }
});
document.addEventListener("keyup", function(hightlight) {
    if(hightlight.key === "Tab") {
    const TabKey = document.querySelector("#tabKey");
    TabKey.style.background = "lightgrey";
    }
});
document.addEventListener("keydown", function(highlight) {
    if(highlight.key.toLowerCase() === "q") {
    const qKey = document.querySelector("#qKey");
    qKey.style.background = "lightblue";
    }
});
document.addEventListener("keyup", function(highlight) {
    if(highlight.key.toLowerCase() === "q") {
    const qKey = document.querySelector("#qKey");
    qKey.style.background = "lightgrey";
    }
});
document.addEventListener("keydown", function(highlight) {
    if(highlight.key.toLowerCase() === "w") {
    const wKey = document.querySelector("#wKey");
    wKey.style.background = "lightblue";
    }
});
document.addEventListener("keyup", function(highlight) {
    if(highlight.key.toLowerCase() === "w") {
    const wKey = document.querySelector("#wKey");
    wKey.style.background = "lightgrey";
    }
});
document.addEventListener("keydown", function(highlight) {
    if(highlight.key.toLowerCase() === "e") {
        const eKey = document.querySelector("#eKey");
        eKey.style.background = "lightblue";
    }
});
document.addEventListener("keyup", function(highlight) {
    if(highlight.key.toLowerCase() === "e") {
        const eKey = document.querySelector("#eKey");
        eKey.style.background = "lightgrey";
    }
});
document.addEventListener("keydown", function(highlight) {
    if(highlight.key.toLowerCase() === "r") {
    const rKey = document.querySelector("#rKey");
    rKey.style.background = "lightblue";
    }
});
document.addEventListener("keyup", function(highlight) {
    if(highlight.key.toLowerCase() === "r") {
    const rKey = document.querySelector("#rKey");
    rKey.style.background = "lightgrey";
    }
});
document.addEventListener("keydown", function(highlight) {
    if(highlight.key.toLowerCase() === "t") {
    const tKey = document.querySelector("#tKey");
    tKey.style.background = "lightblue";
    }
});
document.addEventListener("keyup", function(highlight) {
    if(highlight.key.toLowerCase() === "t") {
    const tKey = document.querySelector("#tKey");
    tKey.style.background = "lightgrey";
    }
});
document.addEventListener("keydown", function(highlight) {
    if(highlight.key.toLowerCase() === "y") {
    const yKey = document.querySelector("#yKey");
    yKey.style.background = "lightblue";
    }
});
document.addEventListener("keyup", function(highlight) {
    if(highlight.key.toLowerCase() === "y") {
    const yKey = document.querySelector("#yKey");
    yKey.style.background = "lightgrey";
    }
});
document.addEventListener("keydown", function(highlight) {
    if(highlight.key.toLowerCase() === "u") {
    const uKey = document.querySelector("#uKey");
    uKey.style.background = "lightblue";
    }
});
document.addEventListener("keyup", function(highlight) {
    if(highlight.key.toLowerCase() === "u") {
    const uKey = document.querySelector("#uKey");
    uKey.style.background = "lightgrey";
    }
});
document.addEventListener("keydown", function(highlight) {
    if(highlight.key.toLowerCase() === "i") {
    const iKey = document.querySelector("#iKey");
    iKey.style.background = "lightblue";
    }
});
document.addEventListener("keyup", function(highlight) {
    if(highlight.key.toLowerCase() === "i") {
    const iKey = document.querySelector("#iKey");
    iKey.style.background = "lightgrey";
    }
});
document.addEventListener("keydown", function(highlight) {
    if(highlight.key.toLowerCase() === "o") {
    const oKey = document.querySelector("#oKey");
    oKey.style.background = "lightblue";
    }
});
document.addEventListener("keyup", function(highlight) {
    if(highlight.key.toLowerCase() === "o") {
    const oKey = document.querySelector("#oKey");
    oKey.style.background = "lightgrey";
    }
});
document.addEventListener("keydown", function(highlight) {
    if(highlight.key.toLowerCase() === "p") {
    const pKey = document.querySelector("#pKey");
    pKey.style.background = "lightblue";
    }
});
document.addEventListener("keyup", function(highlight) {
    if(highlight.key.toLowerCase() === "p") {
    const pKey = document.querySelector("#pKey");
    pKey.style.background = "lightgrey";
    }
});
document.addEventListener("keydown", function(highlight) {
    if(highlight.code === "BracketLeft") {
    const BracketLeftKey = document.querySelector("#opnbracketKey");
    BracketLeftKey.style.background = "lightblue";
    }
});
document.addEventListener("keyup", function(highlight) {
    if(highlight.code === "BracketLeft") {
    const BracketLeftKey = document.querySelector("#opnbracketKey");
    BracketLeftKey.style.background = "lightgrey";
    }
});
document.addEventListener("keydown", function(highlight) {
    if(highlight.code === "BracketRight") {
    const BracketRightKey = document.querySelector("#clsbracketKey");
    BracketRightKey.style.background = "lightblue";
    }
});
document.addEventListener("keyup", function(highlight) {
    if(highlight.code === "BracketRight") {
    const BracketRightKey = document.querySelector("#clsbracketKey");
    BracketRightKey.style.background = "lightgrey";
    }
});
document.addEventListener("keydown", function(highlight) {
    if(highlight.code === "Backslash") {
    const BackslashKey = document.querySelector("#bckslashKey");
    BackslashKey.style.background = "lightblue";
    }
});
document.addEventListener("keyup", function(highlight) {
    if(highlight.code === "Backslash") {
    const BackslashKey = document.querySelector("#bckslashKey");
    BackslashKey.style.background = "lightgrey";
    }
});
/*This is where row 4 starts!!!*/
document.addEventListener("keydown", function(highlight) {
    if(highlight.key === "CapsLock") {
    const CapsLockKey = document.querySelector("#capslockKey");
    CapsLockKey.style.background = "lightblue";
    }
});
document.addEventListener("keyup", function(highlight) {
    if(highlight.key === "CapsLock") {
        const CapsLockKey = document.querySelector("#capslockKey");
        CapsLockKey.style.background = "lightgrey";
    }
});
document.addEventListener("keydown", function(highlight) {
    if(highlight.key.toLowerCase() === "a") {
        const aKey = document.querySelector("#aKey");
        aKey.style.background = "lightblue";
    }
});
document.addEventListener("keyup", function(highlight) {
    if(highlight.key.toLowerCase() === "a") {
        const aKey = document.querySelector("#aKey");
        aKey.style.background = "darkcyan";
    }
});
document.addEventListener("keydown", function(highlight) {
    if(highlight.key.toLowerCase() === "s") {
    const sKey = document.querySelector("#sKey");
    sKey.style.background = "lightblue";
    }
});
document.addEventListener("keyup", function(highlight) {
    if(highlight.key.toLowerCase() === "s") {
    const sKey = document.querySelector("#sKey");
    sKey.style.background = "darkcyan";
    }
});
document.addEventListener("keydown", function(highlight) {
    if(highlight.key.toLowerCase() === "d") {
    const dKey = document.querySelector("#dKey");
    dKey.style.background = "lightblue";
    }
});
document.addEventListener("keyup", function(highlight) {
    if(highlight.key.toLowerCase() === "d") {
    const dKey = document.querySelector("#dKey");
    dKey.style.background = "darkcyan";
    }
});
document.addEventListener("keydown", function(highlight) {
    if(highlight.key.toLowerCase() === "f") {
    const fKey = document.querySelector("#fKey");
    fKey.style.background = "lightblue";
    }
});
document.addEventListener("keyup", function(highlight) {
    if(highlight.key.toLowerCase() === "f") {
    const fKey = document.querySelector("#fKey");
    fKey.style.background = "darkcyan";
    }
});
document.addEventListener("keydown", function(highlight) {
    if(highlight.key.toLowerCase() === "g") {
    const gKey = document.querySelector("#gKey");
    gKey.style.background = "lightblue";
    }
});
document.addEventListener("keyup", function(highlight) {
    if(highlight.key.toLowerCase() === "g") {
    const gKey = document.querySelector("#gKey");
    gKey.style.background = "lightgrey";
    }
});
document.addEventListener("keydown", function(highlight) {
    if(highlight.key.toLowerCase() === "h") {
    const hKey = document.querySelector("#hKey");
    hKey.style.background = "lightblue";
    }
});
document.addEventListener("keyup", function(highlight) {
    if(highlight.key.toLowerCase() === "h") {
    const hKey = document.querySelector("#hKey");
    hKey.style.background = "lightgrey";
    }
});
document.addEventListener("keydown", function(highlight) {
    if(highlight.key.toLowerCase() === "j") {
    const jKey = document.querySelector("#jKey");
    jKey.style.background = "lightblue";
    }
});
document.addEventListener("keyup", function(highlight) {
    if(highlight.key.toLowerCase() === "j") {
    const jKey = document.querySelector("#jKey");
    jKey.style.background = "darkcyan";
    }
});
document.addEventListener("keydown", function(highlight) {
    if(highlight.key.toLowerCase() === "k") {
    const kKey = document.querySelector("#kKey");
    kKey.style.background = "lightblue";
    }
});
document.addEventListener("keyup", function(highlight) {
    if(highlight.key.toLowerCase() === "k") {
    const kKey = document.querySelector("#kKey");
    kKey.style.background = "darkcyan";
    }
});
document.addEventListener("keydown", function(highlight) {
    if(highlight.key.toLowerCase() === "l") {
    const lKey = document.querySelector("#lKey");
    lKey.style.background = "lightblue";
    }
});
document.addEventListener("keyup", function(highlight) {
    if(highlight.key.toLowerCase() === "l") {
    const lKey = document.querySelector("#lKey");
    lKey.style.background = "darkcyan";
    }
});
document.addEventListener("keydown", function(highlight) {
    if(highlight.key.toLowerCase() === ";") {
    const Key = document.querySelector("#semicolKey");
    Key.style.background = "lightblue";
    }
});
document.addEventListener("keyup", function(highlight) {
    if(highlight.key.toLowerCase() === ";") {
    const Key = document.querySelector("#semicolKey");
    Key.style.background = "darkcyan";
    }
});
document.addEventListener("keydown", function(highlight) {
    if(highlight.key.toLowerCase() === "'") {
    const Key = document.querySelector("#quoteKey");
    Key.style.background = "lightblue";
    }
});
document.addEventListener("keyup", function(highlight) {
    if(highlight.key.toLowerCase() === "'") {
    const Key = document.querySelector("#quoteKey");
    Key.style.background = "lightgrey";
    }
});
document.addEventListener("keydown", function(highlight) {
    if(highlight.key === "Enter") {
    const Key = document.querySelector("#enterKey");
    Key.style.background = "lightblue";
    }
});
document.addEventListener("keyup", function(highlight) {
    if(highlight.key === "Enter") {
    const Key = document.querySelector("#enterKey");
    Key.style.background = "lightgrey";
    }
});
document.addEventListener("keydown", function(highlight) {
    if(highlight.key.toLowerCase() === "") {
    const Key = document.querySelector("#Key");
    Key.style.background = "lightblue";
    }
});
document.addEventListener("keyup", function(highlight) {
    if(highlight.key.toLowerCase() === "") {
    const Key = document.querySelector("#Key");
    Key.style.background = "lightgrey";
    }
});
document.addEventListener("keydown", function(highlight) {
    if(highlight.key.toLowerCase() === "") {
    const Key = document.querySelector("#Key");
    Key.style.background = "lightblue";
    }
});
document.addEventListener("keyup", function(highlight) {
    if(highlight.key.toLowerCase() === "") {
    const Key = document.querySelector("#Key");
    Key.style.background = "lightgrey";
    }
});
/*This is where row five starts!!!*/
document.addEventListener("keydown", function(highlight) {
    if(highlight.code === "ShiftLeft") {
    const ShiftLeftKey = document.querySelector("#lftshiftKey");
    ShiftLeftKey.style.background = "lightblue";
    }
});
document.addEventListener("keyup", function(highlight) {
    if(highlight.code === "ShiftLeft") {
    const ShiftLeftKey = document.querySelector("#lftshiftKey");
    ShiftLeftKey.style.background = "lightgrey";
    }
    document.addEventListener("keydown", function(hightlight) {
        if(hightlight.key.toLowerCase() === "z") {
        const zKey = document.querySelector("#zKey");
        zKey.style.background = "lightblue";
        }
    });
    document.addEventListener("keyup", function(hightlight) {
        if(hightlight.key.toLowerCase() === "z") {
        const zKey = document.querySelector("#zKey");
        zKey.style.background = "lightgrey";
        }
    });
    document.addEventListener("keydown", function(highlight) {
        if(highlight.key.toLowerCase() === "x") {
        const xKey = document.querySelector("#xKey");
        xKey.style.background = "lightblue";
        }
    });
    document.addEventListener("keyup", function(highlight) {
        if(highlight.key.toLowerCase() === "x") {
        const xKey = document.querySelector("#xKey");
        xKey.style.background = "lightgrey";
        }
    });
    document.addEventListener("keydown", function(highlight) {
        if(highlight.key.toLowerCase() === "c") {
        const cKey = document.querySelector("#cKey");
        cKey.style.background = "lightblue";
        }
    });
    document.addEventListener("keyup", function(highlight) {
        if(highlight.key.toLowerCase() === "c") {
        const cKey = document.querySelector("#cKey");
        cKey.style.background = "lightgrey";
        }
    });
});document.addEventListener("keydown", function(highlight) {
    if(highlight.key.toLowerCase() === "v") {
    const vKey = document.querySelector("#vKey");
    vKey.style.background = "lightblue";
    }
});
document.addEventListener("keyup", function(highlight) {
    if(highlight.key.toLowerCase() === "v") {
    const vKey = document.querySelector("#vKey");
    vKey.style.background = "lightgrey";
    }
});document.addEventListener("keydown", function(highlight) {
    if(highlight.key.toLowerCase() === "b") {
    const bKey = document.querySelector("#bKey");
    bKey.style.background = "lightblue";
    }
});
document.addEventListener("keyup", function(highlight) {
    if(highlight.key.toLowerCase() === "b") {
    const bKey = document.querySelector("#bKey");
    bKey.style.background = "lightgrey";
    }
});
document.addEventListener("keydown", function(highlight) {
    if(highlight.key.toLowerCase() === "n") {
    const nKey = document.querySelector("#nKey");
    nKey.style.background = "lightblue";
    }
});
document.addEventListener("keyup", function(highlight) {
    if(highlight.key.toLowerCase() === "n") {
    const nKey = document.querySelector("#nKey");
    nKey.style.background = "lightgrey";
    }
});
document.addEventListener("keydown", function(highlight) {
    if(highlight.key.toLowerCase() === "m") {
    const mKey = document.querySelector("#mKey");
    mKey.style.background = "lightblue";
    }
});
document.addEventListener("keyup", function(highlight) {
    if(highlight.key.toLowerCase() === "m") {
    const mKey = document.querySelector("#mKey");
    mKey.style.background = "lightgrey";
    }
});
document.addEventListener("keydown", function(highlight) {
    if(highlight.code === "Comma") {
    const CommaKey = document.querySelector("#commaKey");
    CommaKey.style.background = "lightblue";
    }
});
document.addEventListener("keyup", function(highlight) {
    if(highlight.code === "Comma") {
    const CommaKey = document.querySelector("#commaKey");
    CommaKey.style.background = "lightgrey";
    }
});
document.addEventListener("keydown", function(highlight) {
    if(highlight.code === "Period") {
    const PeriodKey = document.querySelector("#periodKey");
    PeriodKey.style.background = "lightblue";
    }
});
document.addEventListener("keyup", function(highlight) {
    if(highlight.code === "Period") {
    const PeriodKey = document.querySelector("#periodKey");
    PeriodKey.style.background = "lightgrey";
    }
});
document.addEventListener("keydown", function(highlight) {
    if(highlight.key === "/" || (highlight.key === "?" && highlight.shiftKey)) {
    const Key = document.querySelector("#fwdslashKey");
    Key.style.background = "lightblue";
    }
});
document.addEventListener("keyup", function(highlight) {
    if(highlight.key === "/" || (highlight.key === "?" && highlight.shiftKey)) {
    const Key = document.querySelector("#fwdslashKey");
    Key.style.background = "lightgrey";
    }
});
document.addEventListener("keydown", function(highlight) {
    if(highlight.code === "ShiftRight") {
    const ShiftRightKey = document.querySelector("#rightshiftKey");
    ShiftRightKey.style.background = "lightblue";
    }
});
document.addEventListener("keyup", function(highlight) {
    if(highlight.code === "ShiftRight") {
    const ShiftRightKey = document.querySelector("#rightshiftKey");
    ShiftRightKey.style.background = "lightgrey";
    }
});
document.addEventListener("keydown", function(highlight) {
    if(highlight.code === "ControlLeft") {
    const Key = document.querySelector("#lftctrlKey");
    Key.style.background = "lightblue";
    }
});
document.addEventListener("keyup", function(highlight) {
    if(highlight.code === "ControlLeft") {
    const Key = document.querySelector("#lftctrlKey");
    Key.style.background = "lightgrey";
    }
});
document.addEventListener("keydown", function(highlight) {
    if(highlight.code === "AltLeft") {
    const Key = document.querySelector("#lftaltKey");
    Key.style.background = "lightblue";
    }
});
document.addEventListener("keyup", function(highlight) {
    if(highlight.code === "AltLeft") {
    const Key = document.querySelector("#lftaltKey");
    Key.style.background = "lightgrey";
    }
});
document.addEventListener("keydown", function(highlight) {
    if(highlight.code === "Space") {
    const Key = document.querySelector("#spacebarKey");
    Key.style.background = "lightblue";
    }
});
document.addEventListener("keyup", function(highlight) {
    if(highlight.code === "Space") {
    const Key = document.querySelector("#spacebarKey");
    Key.style.background = "darkcyan";
    }
});
document.addEventListener("keydown", function(highlight) {
    if(highlight.code === "AltRight") {
    const Key = document.querySelector("#rightaltKey");
    Key.style.background = "lightblue";
    }
});
document.addEventListener("keyup", function(highlight) {
    if(highlight.code === "AltRight") {
    const Key = document.querySelector("#rightaltKey");
    Key.style.background = "lightgrey";
    }
});
document.addEventListener("keydown", function(highlight) {
    if(highlight.code === "ControlRight") {
    const Key = document.querySelector("#rightctrlKey");
    Key.style.background = "lightblue";
    }
});
document.addEventListener("keyup", function(highlight) {
    if(highlight.code === "ControlRight") {
    const Key = document.querySelector("#rightctrlKey");
    Key.style.background = "lightgrey";
    }
});
document.addEventListener("keydown", function(highlight) {
    if(highlight.code === "ArrowLeft") {
    const Key = document.querySelector("#lftarrKey");
    Key.style.background = "lightblue";
    }
});
document.addEventListener("keyup", function(highlight) {
    if(highlight.code === "ArrowLeft") {
    const Key = document.querySelector("#lftarrKey");
    Key.style.background = "lightgrey";
    }
});
document.addEventListener("keydown", function(highlight) {
    if(highlight.code === "ArrowUp") {
    const Key = document.querySelector("#uparrKey");
    Key.style.background = "lightblue";
    }
});
document.addEventListener("keyup", function(highlight) {
    if(highlight.code === "ArrowUp") {
    const Key = document.querySelector("#uparrKey");
    Key.style.background = "lightgrey";
    }
});
document.addEventListener("keydown", function(highlight) {
    if(highlight.code === "ArrowDown") {
    const Key = document.querySelector("#dwnarrKey");
    Key.style.background = "lightblue";
    }
});
document.addEventListener("keyup", function(highlight) {
    if(highlight.code === "ArrowDown") {
    const Key = document.querySelector("#dwnarrKey");
    Key.style.background = "lightgrey";
    }
});
document.addEventListener("keydown", function(highlight) {
    if(highlight.code === "ArrowRight") {
    const Key = document.querySelector("#rightarrKey");
    Key.style.background = "lightblue";
    }
});
document.addEventListener("keyup", function(highlight) {
    if(highlight.code === "ArrowRight") {
    const Key = document.querySelector("#rightarrKey");
    Key.style.background = "lightgrey";
    }
});
document.addEventListener("keyup", function(highlight) {
    if(highlight.key.toLowerCase() === "") {
    const Key = document.querySelector("#Key");
    Key.style.background = "lightgrey";
    }
});
document.addEventListener("keydown", function(highlight) {
    if(highlight.key.toLowerCase() === "") {
    const Key = document.querySelector("#Key");
    Key.style.background = "lightblue";
    }
});
document.addEventListener("keyup", function(highlight) {
    if(highlight.key.toLowerCase() === "") {
    const Key = document.querySelector("#Key");
    Key.style.background = "lightgrey";
    }
});